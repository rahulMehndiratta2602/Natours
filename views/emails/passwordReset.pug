extends baseEmail 

block content
    p Hi #{firstName},
    p Forgot your password? No worries. Just send a PATCH request to:#{url} with your password and passwordConfirm in request body.
    p (Website for this feature not yet implemented. Use Postman to send request)
    table.btn.btn-primary(role='presentation', border='0', cellpadding='0', cellspacing='0')
        tbody
            tr
            td(align='left')
                table(role='presentation', border='0', cellpadding='0', cellspacing='0')
                    tbody
                        tr
                            td
                                a(href=`${url}`, target='_blank') Reset Password
    p  If you didnt request for a password reset, you can safely ignore this email.